Lab Activity 6: Two-Way Communication (Buttons & Events)

This is an activity about demonstrating bidirectional communication. The Arduino reads physical buttons and sends a signal to the PC when one is pressed and the python script listens for these signals and updates the display or sends commands back to toggle LEDs. It uses a header file (.h) to keep the main code
